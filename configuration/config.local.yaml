profile: local

reflex:
  deploy_url: http://localhost
  frontend_port: 8080

app:
  environment: local
  logging: logging.yaml

  database:
    echo: False # Set to True to log every statement

  graph:
    mode: local
    host: 127.0.0.1
    port: 6379

  authentication:
    server_url: http://localhost:8080
