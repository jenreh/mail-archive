{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the mail-archive desktop shell.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
